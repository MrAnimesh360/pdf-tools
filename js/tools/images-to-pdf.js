UI.register('images-to-pdf',{render(root){root.innerHTML=`<h3>Images → PDF</h3><input id=f type=file accept='image/*' multiple><button id=go>Create PDF</button>`;go.onclick=async()=>{const files=[...f.files,...(window.__droppedFiles||[])].filter(x=>x.type.startsWith('image/'));if(!files.length)return;const out=await PDFEngine.imagesToPdf(files);PDFEngine.download(out,'images.pdf');};}});
